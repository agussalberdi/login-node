<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>Recibidos</title>
    <% include partials/head %>
</head>
<body>
    <% include partials/header %>
    <section id="recibidos">
        <h2 class="mensaje">Mails recibidos</h2>
        <ul class="listado">
            <% for(var i=0; i < recibidos.length; i++) { %>
                <li>
                    <div>
                        <span>Asunto: </span>
                        <span><strong><%= recibidos[i].asunto %></strong></span>
                    </div>
                    <div>
                        <span>Mensaje: </span>
                        <span><strong><%- recibidos[i].mensaje %></strong></span>
                    </div>
                    <div>
                        <span>Emisor del mail: </span>
                        <span><strong><%= recibidos[i].emisor %></strong></span>
                    </div>
                    <div>
                        <span>Receptor del mail: </span>
                        <span><strong><%= recibidos[i].receptor %></strong></span>
                    </div>
                </li>
            <% } %>
        </ul>

        <div class="redirect">
            <p>Volver a tu perfil <a href="/profile">Perfil</a></p>
            <p>O volver a <a href="/">Home</a>.</p>
        </div>
    </section>

    <% include partials/footer %>
</body>
</html>